<h1>AjaxApp</h1>
<%= form_with url:  "/posts", method: :post,id: "form" do |form| %>
  <%= form.text_field :content  , id: "content"%>
  <%# なぜidを追加した？ %>
  <%= form.submit '投稿する' , id: "submit" %>
<% end %>

<div id="list">
<%# メモを追加する要素？？？？ %>
</div>

<% @posts.each do |post|%>
 <div class="post" data-id=<%= post.id %> data-check=<%= post.checked %>>
 <%# 前者はメモのidが入り、後者は既読の有無を管理するcheckedカラムの情報が入る。post.controllerのif文でどちらがtureかで表示が変わる%>
 <%#[data-id]はカスタムデータ
    <div class="post-date">
      投稿日時：<%= post.created_at %>
    </div>
    <div class="post-content">
      <%= post.content %>
    </div>
  </div>
<%end%>